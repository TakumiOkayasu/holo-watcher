{
	"name": "holo-ci-workers",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy",
		"dev": "wrangler dev",
		"start": "wrangler dev",
		"test": "vitest",
		"test:cli": "vitest run --config vitest.cli.config.mts",
		"cf-typegen": "wrangler types",
		"sync-webhooks": "bun run scripts/sync-webhooks.ts"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.8.19",
		"typescript": "^5.5.2",
		"vitest": "~3.2.0",
		"wrangler": "^4.59.2"
	},
	"dependencies": {
		"@anthropic-ai/sdk": "^0.71.2"
	}
}